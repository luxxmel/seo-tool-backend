name: Wake Render Backend

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest

    steps:
      - name: Wake Render
        run: |
          curl -fsSL --max-time 60 https://seo-tool-backend-phw2.onrender.com/api/seo/health
